pipeline {
    agent any

    stages {
        stage('Checkout SCM') {
            steps {
                checkout scm
            }
        }

        stage('Clean Workspace') {
            steps {
                deleteDir()
            }
        }

        stage('Install Dependencies') {
            steps {
                dir('ngrx-project') {  // Change to your project folder
                    retry(2) {
                        bat 'npm install'
                    }
                }
            }
        }

        stage('Build Angular Project') {
            steps {
                dir('ngrx-project') {
                    bat 'npm run build --prod'
                }
            }
        }

        stage('Archive Build') {
            steps {
                dir('ngrx-project') {
                    archiveArtifacts artifacts: 'dist/**', fingerprint: true
                }
            }
        }

        stage('Deploy to IIS') {
            steps {
                bat 'if not exist "C:\\inetpub\\wwwroot\\MyApp" mkdir "C:\\inetpub\\wwwroot\\MyApp"'
                bat 'xcopy /E /Y /I ngrx-project\\dist\\* "C:\\inetpub\\wwwroot\\MyApp\\"'
            }
        }

        stage('Restart IIS') {
            steps {
                bat 'C:\\Windows\\System32\\iisreset.exe'
            }
        }
    }

    post {
        failure {
            echo 'Build or deployment failed! Check console logs.'
        }
    }
}
