pipeline {
    agent any

    environment {
        BUILD_DIR = "dist"
        DEPLOY_DIR = "C:\\inetpub\\wwwroot\\ngrx-project"
    }

    stages {
        stage('Clean Workspace') {
            steps {
                deleteDir() // ✅ Clean first
            }
        }

        stage('Checkout SCM') {
            steps {
                checkout scm // ✅ Then checkout the repo
                bat 'dir'    // ✅ Debug: confirm package.json is present
            }
        }

        stage('Install Dependencies') {
            steps {
                retry(2) {
                    bat 'npm install'
                }
            }
        }

        stage('Build Angular Project') {
            steps {
                bat 'npm run build -- --configuration production'
            }
        }

        stage('Archive Build') {
            steps {
                archiveArtifacts artifacts: "${BUILD_DIR}/**", fingerprint: true
            }
        }

        stage('Deploy to IIS') {
            steps {
                bat "if not exist \"${DEPLOY_DIR}\" mkdir \"${DEPLOY_DIR}\""
                bat "xcopy /E /Y /I ${BUILD_DIR}\\* \"${DEPLOY_DIR}\\\""
            }
        }

        stage('Restart IIS') {
            steps {
                bat 'iisreset'
            }
        }
    }

    post {
        failure {
            echo 'Build or deployment failed! Check console logs.'
        }
    }
}
